# Gitlet
The purpose of Gitlet is to replicate the core functionality of Git, providing a version-control system that allows users to, for example, initialize repositories, track file changes through blobs with SHA-1 content addressing, and support remote repository operations such as push, pull, and fetch.
